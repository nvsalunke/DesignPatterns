using System;

namespace DesignPatterns
{
    class Program
    {
        //Creational Design Patterns: Provides an interface for creating families of related or dependent objects without specifying their concrete classes.
        //Structural Design Patterns: This pattern lets classes work together that could not otherwise because of incompatible interfaces.
        //Behavioral Design Patterns: This pattern defines a chain of processing objects in a chain in such a way that the incoming request is processed by each processing objects in sequence.
        static void Main(string[] args)
        {
            Console.WriteLine("Hello World!");
            Creational.Program.Main(args);
        }
    }
}
